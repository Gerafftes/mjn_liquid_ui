# mjn_liquid_ui_example

Demo app for `mjn_liquid_ui`.

Run it from this directory:

```sh
flutter run -d ios
```

The demo has Home, Jobs, Chat, and Search tabs. On supported iOS versions the Search tab is rendered by the native SwiftUI `Tab(..., role: .search)` implementation.
